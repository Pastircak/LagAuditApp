import SwiftUI

struct DetachersView: View {
    var body: some View {
        VStack {
            Text("Detachers Section")
                .font(.title)
                .padding()
            Spacer()
        }
    }
} 
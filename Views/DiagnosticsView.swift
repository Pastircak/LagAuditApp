import SwiftUI

struct DiagnosticsView: View {
    var body: some View {
        VStack {
            Text("Diagnostics Section")
                .font(.title)
                .padding()
            Spacer()
        }
    }
} 
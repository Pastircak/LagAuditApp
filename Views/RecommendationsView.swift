import SwiftUI

struct RecommendationsView: View {
    var body: some View {
        VStack {
            Text("Recommendations Section")
                .font(.title)
                .padding()
            Spacer()
        }
    }
} 
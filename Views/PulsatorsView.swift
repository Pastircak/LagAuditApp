import SwiftUI

struct PulsatorsView: View {
    var body: some View {
        VStack {
            Text("Pulsators Section")
                .font(.title)
                .padding()
            Spacer()
        }
    }
} 
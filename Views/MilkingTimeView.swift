import SwiftUI

struct MilkingTimeView: View {
    var body: some View {
        VStack {
            Text("Milking Time Section")
                .font(.title)
                .padding()
            Spacer()
        }
    }
} 
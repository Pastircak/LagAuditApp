import SwiftUI

struct OverviewView: View {
    var body: some View {
        VStack {
            Text("Overview Section")
                .font(.title)
                .padding()
            Spacer()
        }
    }
} 